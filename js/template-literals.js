const name = prompt('Whats your name?');
// const message = "Hello " + name;


const message = `Hello, ${name}. Welcome to my music site. I'm happy
 that you came by to visit, ${name}. 
 Please come again and listen to more music!`;

 console.log(message);

 /**
  * define template Literals with backticks
  * a dollar sign with curly braces marks the dynamic values in a template literal.
  * 
  * interpolation:
  * Whatever's inside the curly braces gets evaluated by the JavaScript engine and
  * added to the string. his process is known as interpolation.
  * 
  */